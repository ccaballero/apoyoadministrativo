{% extends 'CidetsiBaseBundle:Crud:read.html.twig' %}

{% block body %}
<section>
    <div class="container">
        {{ include('CidetsiBaseBundle:Block:flashbag.html.twig') }}
        <h1>{{ entity.name }}</h1>
        <p class="lead">{{ entity.departamento }}</p>
        {{ include('CidetsiDepartamentosBundle:Carrera:Tasks/element.html.twig', {
            'view': false,  'edit': true, 'delete': true, 'admin': true
        }) }}
    
        <h2>Agregar plan de estudio</h2>
        {{ form(form) }}
    </div>
</section>
{% endblock %}
