{% for plan in entity.planes %}
    <h3>{{ plan.name }}</h3>
    <ul class="list-group">
    {% for materia in plan.materias %}
        <li class="list-group-item">
            <span>{{ materia.name }} ({{ materia.code}})</span>
        </li>
    {% endfor %}
    </ul>
{% endfor %}
