{% extends '::base.html.twig' %}

{% block breadcrumb %}
<ol class="breadcrumb">
    <li class="active">Departamentos</li>
</ol>
{% endblock %}

{% block body %}
<section>
    <div class="container">
        <h1>Departamentos</h1>
        <p>Estos son los departamentos en la facultad de ciencias y tecnologia,
        para los que este sistema gestiona información.</p>
        <div class="panel-group" id="accordion">
        {% for item in list %}
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h2 class="panel-title">
                        <a data-toggle="collapse" data-parent="#accordion"
                           href="#collapse{{ item.id }}">
                            {{ item.name }}
                        </a>
                    </h2>
                </div>
                <div id="collapse{{ item.id }}" class="panel-collapse collapse">
                    <div class="panel-body">
                        <h3>Carreras</h3>
                        <ul class="list-group">
                        {% for carrera in item.carreras %}
                            <li class="list-group-item">
                                {{ carrera.name }} ({{ carrera.abbreviation}})
                            </li>
                        {% endfor %}
                        </ul>
                        <button type="button" class="btn btn-primary">Ver detalles</button>
                    </div>
                </div>
            </div>
        {% endfor %}
        </div>
    </div>
</section>
{% endblock %}
