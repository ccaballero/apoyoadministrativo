{% extends '::base.html.twig' %}

{% block breadcrumb %}
<ol class="breadcrumb">
    <li><a href="{{ path(url_list) }}">{{ title_list }}</a></li>
    <li><a href="{{ path(url_element, { 'id' : entity.id }) }}">{{ entity.name }}</a></li>
    <li><span>Edición</span></li>
</ol>
{% endblock %}

{% block body %}
<section>
    <div class="container">
        <h1>Editar departamento</h1>
        {{ form(edit_form) }}
    </div>
</section>
{% endblock %}
